"""Authentication and authorisation package.

Provides FastAPI dependencies for:

- API key validation via the ``X-API-Key`` request header
- Webhook HMAC signature verification for incoming GitHub webhook events
"""
