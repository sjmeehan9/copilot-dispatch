"""CDK stacks package.

Defines the AWS CDK stack(s) used to provision the Copilot Dispatch infrastructure:

- ``CopilotDispatchStack`` — AppRunner service, DynamoDB table, ECR repository, and
  the necessary IAM roles and policies
"""
